// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: invoices.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const invoiceGet = `-- name: InvoiceGet :one
SELECT id, tenant_id, branch_id, child_id, billing_month, invoice_kind, status,
       invoice_number, issued_sequence, generated_run_id, issued_run_id,
       issued_at, issued_by_user_id, issued_by_membership_id, locked_at,
       due_at, currency_code, subtotal_minor, funded_deduction_minor, total_due_minor,
       amount_paid_minor, paid_at, payment_failed_at, payment_status_updated_at,
       adjusts_invoice_id, adjustment_reason_code, adjustment_reason_note,
       period_start_date, period_end_date, calculation_details,
       created_at, updated_at
FROM invoices
WHERE tenant_id = $1 AND branch_id = $2 AND id = $3
`

type InvoiceGetParams struct {
	TenantID pgtype.UUID
	BranchID pgtype.UUID
	ID       pgtype.UUID
}

func (q *Queries) InvoiceGet(ctx context.Context, arg InvoiceGetParams) (Invoice, error) {
	row := q.db.QueryRow(ctx, invoiceGet, arg.TenantID, arg.BranchID, arg.ID)
	var i Invoice
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.BranchID,
		&i.ChildID,
		&i.BillingMonth,
		&i.InvoiceKind,
		&i.Status,
		&i.InvoiceNumber,
		&i.IssuedSequence,
		&i.GeneratedRunID,
		&i.IssuedRunID,
		&i.IssuedAt,
		&i.IssuedByUserID,
		&i.IssuedByMembershipID,
		&i.LockedAt,
		&i.DueAt,
		&i.CurrencyCode,
		&i.SubtotalMinor,
		&i.FundedDeductionMinor,
		&i.TotalDueMinor,
		&i.AmountPaidMinor,
		&i.PaidAt,
		&i.PaymentFailedAt,
		&i.PaymentStatusUpdatedAt,
		&i.AdjustsInvoiceID,
		&i.AdjustmentReasonCode,
		&i.AdjustmentReasonNote,
		&i.PeriodStartDate,
		&i.PeriodEndDate,
		&i.CalculationDetails,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const invoiceLineListByInvoice = `-- name: InvoiceLineListByInvoice :many
SELECT id, tenant_id, branch_id, invoice_id, line_kind, description, sort_order,
       quantity_minutes, unit_amount_minor, line_amount_minor,
       raw_attended_minutes, rounded_attended_minutes, funded_allowance_minutes,
       funded_deduction_minutes, core_billable_minutes, session_count, details,
       created_at, updated_at
FROM invoice_lines
WHERE tenant_id = $1 AND branch_id = $2 AND invoice_id = $3
ORDER BY sort_order
`

type InvoiceLineListByInvoiceParams struct {
	TenantID  pgtype.UUID
	BranchID  pgtype.UUID
	InvoiceID pgtype.UUID
}

func (q *Queries) InvoiceLineListByInvoice(ctx context.Context, arg InvoiceLineListByInvoiceParams) ([]InvoiceLine, error) {
	rows, err := q.db.Query(ctx, invoiceLineListByInvoice, arg.TenantID, arg.BranchID, arg.InvoiceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []InvoiceLine
	for rows.Next() {
		var i InvoiceLine
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.BranchID,
			&i.InvoiceID,
			&i.LineKind,
			&i.Description,
			&i.SortOrder,
			&i.QuantityMinutes,
			&i.UnitAmountMinor,
			&i.LineAmountMinor,
			&i.RawAttendedMinutes,
			&i.RoundedAttendedMinutes,
			&i.FundedAllowanceMinutes,
			&i.FundedDeductionMinutes,
			&i.CoreBillableMinutes,
			&i.SessionCount,
			&i.Details,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const invoiceNumberSequenceGetForUpdate = `-- name: InvoiceNumberSequenceGetForUpdate :one
SELECT tenant_id, branch_id, billing_year, billing_month, next_sequence, created_at, updated_at
FROM invoice_number_sequences
WHERE tenant_id = $1 AND branch_id = $2 AND billing_year = $3 AND billing_month = $4
FOR UPDATE
`

type InvoiceNumberSequenceGetForUpdateParams struct {
	TenantID     pgtype.UUID
	BranchID     pgtype.UUID
	BillingYear  int32
	BillingMonth int32
}

func (q *Queries) InvoiceNumberSequenceGetForUpdate(ctx context.Context, arg InvoiceNumberSequenceGetForUpdateParams) (InvoiceNumberSequence, error) {
	row := q.db.QueryRow(ctx, invoiceNumberSequenceGetForUpdate,
		arg.TenantID,
		arg.BranchID,
		arg.BillingYear,
		arg.BillingMonth,
	)
	var i InvoiceNumberSequence
	err := row.Scan(
		&i.TenantID,
		&i.BranchID,
		&i.BillingYear,
		&i.BillingMonth,
		&i.NextSequence,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const invoiceRunGet = `-- name: InvoiceRunGet :one
SELECT id, tenant_id, branch_id, billing_month, run_type, status, started_at, completed_at,
       requested_by_user_id, requested_by_membership_id, request_id,
       eligible_count, success_count, blocked_count, failed_count, details,
       created_at, updated_at
FROM invoice_runs
WHERE tenant_id = $1 AND branch_id = $2 AND id = $3
`

type InvoiceRunGetParams struct {
	TenantID pgtype.UUID
	BranchID pgtype.UUID
	ID       pgtype.UUID
}

func (q *Queries) InvoiceRunGet(ctx context.Context, arg InvoiceRunGetParams) (InvoiceRun, error) {
	row := q.db.QueryRow(ctx, invoiceRunGet, arg.TenantID, arg.BranchID, arg.ID)
	var i InvoiceRun
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.BranchID,
		&i.BillingMonth,
		&i.RunType,
		&i.Status,
		&i.StartedAt,
		&i.CompletedAt,
		&i.RequestedByUserID,
		&i.RequestedByMembershipID,
		&i.RequestID,
		&i.EligibleCount,
		&i.SuccessCount,
		&i.BlockedCount,
		&i.FailedCount,
		&i.Details,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const preflightListAttendanceSessions = `-- name: PreflightListAttendanceSessions :many
SELECT
    id,
    child_id,
    status,
    check_in_at,
    check_out_at,
    check_in_local_date,
    check_out_local_date
FROM attendance_sessions
WHERE tenant_id = $1
  AND branch_id = $2
  AND check_in_local_date >= $3
  AND check_in_local_date < $4
  AND status IN ('open', 'complete', 'corrected')
ORDER BY child_id, check_in_local_date, check_in_at, id
`

type PreflightListAttendanceSessionsParams struct {
	TenantID           pgtype.UUID
	BranchID           pgtype.UUID
	CheckInLocalDate   pgtype.Date
	CheckInLocalDate_2 pgtype.Date
}

type PreflightListAttendanceSessionsRow struct {
	ID                pgtype.UUID
	ChildID           pgtype.UUID
	Status            string
	CheckInAt         pgtype.Timestamptz
	CheckOutAt        pgtype.Timestamptz
	CheckInLocalDate  pgtype.Date
	CheckOutLocalDate pgtype.Date
}

func (q *Queries) PreflightListAttendanceSessions(ctx context.Context, arg PreflightListAttendanceSessionsParams) ([]PreflightListAttendanceSessionsRow, error) {
	rows, err := q.db.Query(ctx, preflightListAttendanceSessions,
		arg.TenantID,
		arg.BranchID,
		arg.CheckInLocalDate,
		arg.CheckInLocalDate_2,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PreflightListAttendanceSessionsRow
	for rows.Next() {
		var i PreflightListAttendanceSessionsRow
		if err := rows.Scan(
			&i.ID,
			&i.ChildID,
			&i.Status,
			&i.CheckInAt,
			&i.CheckOutAt,
			&i.CheckInLocalDate,
			&i.CheckOutLocalDate,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const preflightListChildren = `-- name: PreflightListChildren :many
SELECT
    c.id AS child_id,
    c.full_name,
    c.date_of_birth,
    c.start_date,
    c.end_date,
    c.core_hourly_rate_minor,
    EXISTS (
        SELECT 1
        FROM guardian_child_links gcl
        WHERE gcl.tenant_id = c.tenant_id
          AND gcl.branch_id = c.branch_id
          AND gcl.child_id = c.id
          AND gcl.ended_at IS NULL
    ) AS has_guardian_link,
    fp.id AS funding_profile_id,
    fp.funded_allowance_minutes,
    i.id AS existing_invoice_id,
    i.status AS existing_invoice_status
FROM children c
LEFT JOIN funding_profiles fp
    ON fp.tenant_id = c.tenant_id
    AND fp.branch_id = c.branch_id
    AND fp.child_id = c.id
    AND fp.billing_month = $3
LEFT JOIN invoices i
    ON i.tenant_id = c.tenant_id
    AND i.branch_id = c.branch_id
    AND i.child_id = c.id
    AND i.billing_month = $3
    AND i.invoice_kind = 'monthly'
WHERE c.tenant_id = $1
  AND c.branch_id = $2
  AND c.start_date < $4
  AND (c.end_date IS NULL OR c.end_date >= $3)
ORDER BY c.full_name, c.id
`

type PreflightListChildrenParams struct {
	TenantID     pgtype.UUID
	BranchID     pgtype.UUID
	BillingMonth pgtype.Date
	StartDate    pgtype.Date
}

type PreflightListChildrenRow struct {
	ChildID                pgtype.UUID
	FullName               string
	DateOfBirth            pgtype.Date
	StartDate              pgtype.Date
	EndDate                pgtype.Date
	CoreHourlyRateMinor    int32
	HasGuardianLink        bool
	FundingProfileID       pgtype.UUID
	FundedAllowanceMinutes pgtype.Int4
	ExistingInvoiceID      pgtype.UUID
	ExistingInvoiceStatus  pgtype.Text
}

func (q *Queries) PreflightListChildren(ctx context.Context, arg PreflightListChildrenParams) ([]PreflightListChildrenRow, error) {
	rows, err := q.db.Query(ctx, preflightListChildren,
		arg.TenantID,
		arg.BranchID,
		arg.BillingMonth,
		arg.StartDate,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PreflightListChildrenRow
	for rows.Next() {
		var i PreflightListChildrenRow
		if err := rows.Scan(
			&i.ChildID,
			&i.FullName,
			&i.DateOfBirth,
			&i.StartDate,
			&i.EndDate,
			&i.CoreHourlyRateMinor,
			&i.HasGuardianLink,
			&i.FundingProfileID,
			&i.FundedAllowanceMinutes,
			&i.ExistingInvoiceID,
			&i.ExistingInvoiceStatus,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
