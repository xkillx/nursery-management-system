// The guardians and guardian_child_links concepts were removed. The
// parent-child link concept is now a child_contacts row of type
// parent_carer plus a parent_membership_children row, both defined in
// their own modules. This file is intentionally empty.
export {};
